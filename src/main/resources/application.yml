spring:
  datasource:
    type: org.postgresql.jdbc2.optional.SimpleDataSource
    username: postgres
    password: password
    url: jdbc:postgresql://localhost:5432/software

  jpa:
    hibernate:
      ddl-auto: update